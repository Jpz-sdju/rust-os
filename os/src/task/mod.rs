use crate::batch::run_next_app;
use lazy_static::*;

// struct TaskManager {
//     callee_saved : [usize;],

// }

lazy_static! {

}












pub fn suspend_and_run_next() {
    // set_this_suspend();
    // run_next_app();
}